

<form method="post" action="{{ path('board_delete', {'tournament_id': tournament.id, 'id': board.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ board.id) }}">
    <button class="btn btn-danger float-right"><i class="far fa-trash-alt mr-2"></i>Supprimer</button>
</form>
