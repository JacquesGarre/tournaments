{% extends 'base.html.twig' %}

{% block title %}Nouvau paiement{% endblock %}

{% block body %}
<div class="mt-5 mr-5 ml-5 pt-2 pb-5 bg-light card">

    <div class="card-body">
        <h1 class="h3 mb-5 card-title"><i class="fas fa-plus mr-2"></i>Ajouter un paiement</h1>

        {{ include('payment/_form.html.twig') }}

        <a href="{{ path('tournament_payments', {'tournament_id': tournament.id}) }}" class="btn btn-danger float-right">Annuler</a>
    </div>
</div>
{% endblock %}
