

<form id="delete-form" method="post" action="{{ path('tournament_delete', {'id': tournament.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ tournament.id) }}">
    <button class="btn tournament-menu-item">
        <div class="card shadow-sm">
            <div class="card-header">
                <h4 class="my-0 font-weight-normal">Supprimer</h4>
            </div>
            <div class="card-body">
                <h2 class="text-center"><i class="far fa-trash-alt"></i></h2>
                <ul class="list-unstyled mt-2 mb-2">
                    <li class="font-italic">Supprime le tournoi et toutes ses données</li>
                    <li class="font-italic">(Tableaux, matchs...)</li>
                </ul>
            </div>
        </div>
    </button>
</form>

